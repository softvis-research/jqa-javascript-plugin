/*
    Function with a parameter list which consists of three numbers (x,y,z), a string (str) and an object with an key called name.
*/
function longSignatureFunction(x,y,z,str,obj){
    var sum = x + y + z;
    var stmt = "Hello" + str + "!";
    var partOfObj = obj.name();

    return NULL;
}